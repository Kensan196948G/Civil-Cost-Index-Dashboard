services:
  db:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: cci
      POSTGRES_USER: cci
      POSTGRES_PASSWORD: ${CCI_DB_PASSWORD:?run ./scripts/init-local-env.sh first}
    ports:
      - "127.0.0.1:${CCI_DB_HOST_PORT:-15432}:5432"
    volumes:
      - cci_postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cci -d cci"]
      interval: 5s
      timeout: 5s
      retries: 20

  migrate:
    image: civil-cost-index-dashboard-api:latest
    build:
      context: ./apps/api
    environment:
      APP_ENV: ${APP_ENV:-production}
      SEED_SAMPLE_DATA: ${SEED_SAMPLE_DATA:-false}
      DATABASE_URL: ${CCI_LOCAL_DATABASE_URL:?run ./scripts/init-local-env.sh first}
      DATABASE_URL_DIRECT: ${CCI_LOCAL_DATABASE_URL:?run ./scripts/init-local-env.sh first}
    command: ["sh", "-c", "node scripts/migrate.mjs && node scripts/seed.mjs"]
    volumes:
      - ./data:/data:ro
    depends_on:
      db:
        condition: service_healthy
    restart: "no"

  api:
    image: civil-cost-index-dashboard-api:latest
    build:
      context: ./apps/api
    environment:
      APP_ENV: ${APP_ENV:-production}
      APP_VERSION: ${APP_VERSION:-0.1.0}
      PORT: 8000
      API_HOST: 0.0.0.0
      DATABASE_URL: ${CCI_LOCAL_DATABASE_URL:?run ./scripts/init-local-env.sh first}
      ADMIN_API_KEY: ${ADMIN_API_KEY:-}
      FETCH_ALLOWED_HOSTS: ${FETCH_ALLOWED_HOSTS:-}
      BASIC_AUTH_USERNAME: ${BASIC_AUTH_USERNAME:-}
      BASIC_AUTH_PASSWORD: ${BASIC_AUTH_PASSWORD:-}
      ALLOW_ANONYMOUS_VIEWER: ${ALLOW_ANONYMOUS_VIEWER:-false}
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE:-180}
      REQUEST_LOGGING: ${REQUEST_LOGGING:-true}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3000}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      DEEPSEEK_MODEL: ${DEEPSEEK_MODEL:-}
      PERPLEXITY_API_KEY: ${PERPLEXITY_API_KEY:-}
      PERPLEXITY_MODEL: ${PERPLEXITY_MODEL:-}
      AI_PROVIDER: ${AI_PROVIDER:-}
      AI_MODEL: ${AI_MODEL:-}
      NOTIFY_TEAMS_URL: ${NOTIFY_TEAMS_URL:-}
      NOTIFY_SLACK_URL: ${NOTIFY_SLACK_URL:-}
      CF_ACCESS_TEAM_DOMAIN: ${CF_ACCESS_TEAM_DOMAIN:-}
      CF_ACCESS_AUD: ${CF_ACCESS_AUD:-}
      PDF_CJK_FONT_URL: ${PDF_CJK_FONT_URL:-}
    ports:
      - "${CCI_API_HOST_PORT:-8000}:8000"
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:8000/api/health/ready').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 15s
      timeout: 5s
      retries: 10
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

  scheduler:
    image: civil-cost-index-dashboard-api:latest
    build:
      context: ./apps/api
    command: ["node", "dist-node/scheduler-runner.mjs"]
    environment:
      APP_VERSION: ${APP_VERSION:-0.1.0}
      DATABASE_URL: ${CCI_LOCAL_DATABASE_URL:?run ./scripts/init-local-env.sh first}
      FETCH_ALLOWED_HOSTS: ${FETCH_ALLOWED_HOSTS:-}
      NOTIFY_TEAMS_URL: ${NOTIFY_TEAMS_URL:-}
      NOTIFY_SLACK_URL: ${NOTIFY_SLACK_URL:-}
      SCHEDULER_INTERVAL_SECONDS: ${SCHEDULER_INTERVAL_SECONDS:-300}
      SCHEDULER_RETRY_SECONDS: ${SCHEDULER_RETRY_SECONDS:-3600}
      SCHEDULER_LEASE_SECONDS: ${SCHEDULER_LEASE_SECONDS:-900}
    restart: unless-stopped
    depends_on:
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "node", "-e", "const fs=require('fs');const age=Date.now()-Number(fs.readFileSync('/tmp/cci-scheduler-heartbeat','utf8'));const max=(Number(process.env.SCHEDULER_INTERVAL_SECONDS||300)+120)*1000;process.exit(age<=max?0:1)"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

  web:
    image: civil-cost-index-dashboard-web:latest
    build:
      context: ./apps/web
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-}
        API_PROXY_TARGET: ${API_PROXY_TARGET:-http://api:8000}
    environment:
      PORT: 3000
      HOSTNAME: 0.0.0.0
    ports:
      - "${CCI_WEB_HOST_PORT:-3000}:3000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000/').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 15s
      timeout: 5s
      retries: 10
    depends_on:
      api:
        condition: service_healthy
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

volumes:
  cci_postgres_data:
